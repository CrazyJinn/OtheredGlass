# 他者之镜 Godot 框架 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `99_game/` 下产出一个可运行的 Godot 4 工程，用现成示例剧本 `chapter01_新皮肤.json` 跑通"读 JSON → 解释执行 → 立绘/背景/BGM 演出 → 分支跳转 → 结局"全管线。

**Architecture:** 集中式 `ScriptInterpreter`（autoload 单例）持有全部运行时状态，`match op` 分发 11 条指令；`Game.tscn` 纯渲染订阅信号。剧本只写逻辑名，经 `manifest.json` 映射到资源；美术全部程序占位。

**Tech Stack:** Godot 4.3+ / GDScript 2.0；GUT（Godot Unit Test，dev 依赖）做引擎内单测；Python 3 + `jsonschema` 做数据层校验（本环境可跑）。

## Global Constraints

- Godot 4.3+，GDScript 2.0；目标分辨率 **1536×1024**（3:2）；对话立绘 **400×800**。
- 剧本格式严格遵循 `99_game/data/剧本.schema.json`（复制自 `00_init/剧本.schema.json`）。
- 命名：逻辑名用中文（如 `陈默.沉重`），脚本/类名用 ASCII（PascalCase）。
- 资源缺失静默 fallback：图片→占位图、音频→`push_warning` 后跳过，**不阻塞管线**。
- UI 采用代码动态构建（脚本 `_ready()` 内 `new()` 控件）；`.tscn` 仅放根节点 + attach 脚本。
- 无第三方运行时插件；GUT 仅作 dev 依赖放 `addons/gut/`。
- **环境约束**：本会话无 Godot CLI；GDScript/GUT 测试需在 Godot 编辑器运行，Python 校验可在本 bash 环境运行。
- 不实现 V1 边界外内容（变量/flag、条件分支、特效、独立 CG 指令、多图层场景、配音）。

---

## File Structure

| 文件 | 职责 |
|------|------|
| `99_game/project.godot` | 工程配置、autoload 注册、窗口分辨率 |
| `99_game/README.md` | 打开/运行/验收步骤 |
| `99_game/scripts/autoload/GameManager.gd` | 全局流程（标题↔游戏↔结局）、键位、设置 |
| `99_game/scripts/autoload/ScriptInterpreter.gd` | ★ 剧本解释器：执行循环、指令分发、跳转寻址、立绘槽 |
| `99_game/scripts/autoload/AudioManager.gd` | bgm play/stop/fade、sfx |
| `99_game/scripts/autoload/SaveManager.gd` | 选择点/场景段头自动存档 + 手动槽 |
| `99_game/scripts/data/Manifest.gd` | 加载 `manifest.json`，逻辑名→资源路径 |
| `99_game/scripts/data/ChapterLoader.gd` | 加载/缓存章节 JSON + 结构校验 |
| `99_game/scripts/util/PlaceholderGen.gd` | 程序占位图生成（立绘/场景/头像）+ 缓存 |
| `99_game/scripts/ui/DialogueBox.gd` | 底部对话框：头像/名称/打字机/继续指示 |
| `99_game/scripts/ui/PortraitLayer.gd` | 左/中/右立绘累积渲染 + 入离场 |
| `99_game/scripts/ui/ChoiceMenu.gd` | 选项面板 |
| `99_game/scripts/ui/Backlog.gd` | 对话历史 Log |
| `99_game/scripts/ui/SystemMenu.gd` | 存档/读档/设置/返回标题 |
| `99_game/scripts/ui/SettingsPanel.gd` | 设置项面板 |
| `99_game/scenes/Title.tscn` + `scripts/scenes/Title.gd` | 标题/主菜单 |
| `99_game/scenes/Game.tscn` + `scripts/scenes/Game.gd` | 主游戏场景（背景/立绘/对话框/选项层） |
| `99_game/scenes/Ending.tscn` + `scripts/scenes/Ending.gd` | 结局画面 |
| `99_game/data/chapters/chapter01_新皮肤.json` | 现成示例剧本 |
| `99_game/data/manifest.json` | 逻辑名→占位资源映射 |
| `99_game/data/剧本.schema.json` | schema（复制自 00_init） |
| `99_game/tools/validate_chapter.py` | Python schema 校验脚本（本环境可跑） |
| `99_game/tools/test_validate.py` | 上述脚本的 pytest 测试 |
| `99_game/tests/` | GUT 测试脚本（解释器/加载器/manifest） |

**接口约定（跨任务契约）**：
- `Manifest.get_portrait(logical_name) -> String`（资源路径，缺失返回 `""`）
- `Manifest.get_scene(name) / get_bgm(track) / get_sfx(track) / get_cg(name) -> String`
- `ChapterLoader.load_chapter(file_name) -> Dictionary`（缓存，失败 `push_error` 返回 `{}`）
- `ScriptInterpreter` 信号：`line_ready(kind, payload)`、`portrait_changed(slots: Dictionary)`、`bg_changed(scene_name, time)`、`bgm_changed(track, mode, loop)`、`choice_presented(options)`、`ended(kind, title, cg)`、`chapter_finished()`
- `ScriptInterpreter.start(file: String, scene_id: String = "", label: String = "") -> void`
- `ScriptInterpreter.advance() -> void`（玩家输入后推进）

---

## Task 1: 工程骨架 + 数据文件 + Python schema 校验

**Files:**
- Create: `99_game/project.godot`
- Create: `99_game/README.md`
- Create: `99_game/data/剧本.schema.json`
- Create: `99_game/data/chapters/chapter01_新皮肤.json`
- Create: `99_game/data/manifest.json`
- Create: `99_game/tools/validate_chapter.py`
- Create: `99_game/tools/test_validate.py`
- Create: `99_game/tools/requirements.txt`

**Interfaces:**
- Produces: `tools/validate_chapter.py` 暴露 `validate_chapter(path: str, schema_path: str) -> tuple[bool, list[str]]`，供后续 CI/手动校验复用。

- [ ] **Step 1: 写 Python 校验脚本（这是本环境唯一能立即跑的验证锚点）**

Create `99_game/tools/validate_chapter.py`:

```python
"""剧本章节 JSON Schema 校验。本环境（bash + python）可立即运行。"""
import json
import sys
from pathlib import Path

try:
    import jsonschema
except ImportError:
    sys.stderr.write("缺少依赖：pip install -r tools/requirements.txt\n")
    raise


def validate_chapter(path: str, schema_path: str) -> tuple[bool, list[str]]:
    """返回 (是否通过, 错误消息列表)。"""
    errors: list[str] = []
    try:
        with open(path, "r", encoding="utf-8") as f:
            doc = json.load(f)
        with open(schema_path, "r", encoding="utf-8") as f:
            schema = json.load(f)
    except (OSError, json.JSONDecodeError) as e:
        return False, [f"读取/解析失败: {e}"]

    validator = jsonschema.Draft202012Validator(schema)
    for err in validator.iter_errors(doc):
        loc = "/".join(str(p) for p in err.absolute_path) or "<root>"
        errors.append(f"{loc}: {err.message}")
    return (len(errors) == 0), errors


def main(argv: list[str]) -> int:
    if len(argv) != 3:
        sys.stderr.write("用法: python validate_chapter.py <chapter.json> <schema.json>\n")
        return 2
    ok, errors = validate_chapter(argv[1], argv[2])
    if ok:
        print(f"OK: {argv[1]} 通过 schema 校验")
        return 0
    print(f"FAIL: {argv[1]}")
    for e in errors:
        print("  - " + e)
    return 1


if __name__ == "__main__":
    sys.exit(main(sys.argv))
```

- [ ] **Step 2: 写 requirements 与 pytest 测试**

Create `99_game/tools/requirements.txt`:
```
jsonschema>=4.20
pytest>=7.0
```

Create `99_game/tools/test_validate.py`:
```python
import json
from pathlib import Path
from validate_chapter import validate_chapter

ROOT = Path(__file__).resolve().parent.parent
SCHEMA = str(ROOT / "data" / "剧本.schema.json")
CHAPTER = str(ROOT / "data" / "chapters" / "chapter01_新皮肤.json")


def test_valid_chapter_passes():
    ok, errors = validate_chapter(CHAPTER, SCHEMA)
    assert ok, errors


def test_missing_op_field_fails(tmp_path):
    bad = tmp_path / "bad.json"
    bad.write_text(
        json.dumps({"meta": {"chapter": 1, "title": "x"},
                    "scenes": [{"id": "s", "scene": "x", "lines": [{"text": "hi"}]}]},
                   ensure_ascii=False),
        encoding="utf-8",
    )
    ok, errors = validate_chapter(str(bad), SCHEMA)
    assert not ok
    assert any("op" in e or "required" in e for e in errors)


def test_bad_ending_kind_fails(tmp_path):
    bad = tmp_path / "bad.json"
    bad.write_text(
        json.dumps({"meta": {"chapter": 1, "title": "x"},
                    "scenes": [{"id": "s", "scene": "x",
                                "lines": [{"op": "ending", "kind": "XXX"}]}]},
                   ensure_ascii=False),
        encoding="utf-8",
    )
    ok, _ = validate_chapter(str(bad), SCHEMA)
    assert not ok
```

- [ ] **Step 3: 复制 schema 与示例剧本、写 manifest**

复制 `00_init/剧本.schema.json` → `99_game/data/剧本.schema.json`（字节相同）。

复制 `00_init/剧本.md` 中的完整 `chapter01_新皮肤.json` 示例 → `99_game/data/chapters/chapter01_新皮肤.json`，内容如下（与 spec 第 5.2 节一致）：

```json
{
  "meta": {
    "chapter": 1,
    "title": "新皮肤·Day0",
    "requires": {
      "characters": ["陈默"],
      "scenes": ["长江大桥-栏杆", "出租屋"],
      "portraits": ["陈默.沉重", "陈默.释然", "陈默.疲惫"]
    }
  },
  "scenes": [
    {
      "id": "桥上",
      "scene": "长江大桥-栏杆",
      "time": "夜晚",
      "bgm": { "track": "夜风", "mode": "play", "loop": true },
      "lines": [
        { "op": "say", "who": "陈默", "portrait": "沉重", "pos": "center", "text": "就到这里吧。" },
        { "op": "narrate", "text": "江风很大。他翻过栏杆。" },
        { "op": "choice", "options": [
            { "label": "再想想", "to": "keepgoing" },
            { "label": "跳下去", "scene": "结局_BE", "leads_to_ending": true }
          ]
        },
        { "op": "label", "name": "keepgoing" },
        { "op": "say", "who": "陈默", "portrait": "释然", "pos": "center", "text": "……电话响了。" },
        { "op": "jump", "scene": "回出租屋" }
      ]
    },
    {
      "id": "回出租屋",
      "scene": "出租屋",
      "time": "凌晨",
      "lines": [
        { "op": "narrate", "text": "步行回八楼，身体像被掏空。" },
        { "op": "say", "who": "陈默", "portrait": "疲惫", "pos": "center", "text": "……回来了。" }
      ]
    },
    {
      "id": "结局_BE",
      "scene": "长江大桥-栏杆",
      "time": "夜晚",
      "lines": [
        { "op": "narrate", "text": "第二天，很多人自发买了吃的来祭奠。" },
        { "op": "ending", "kind": "BE", "title": "无人知晓的纵身" }
      ]
    }
  ]
}
```

Create `99_game/data/manifest.json`:
```json
{
  "portraits": {
    "陈默.沉重": "assets/portraits/陈默.沉重.png",
    "陈默.释然": "assets/portraits/陈默.释然.png",
    "陈默.疲惫": "assets/portraits/陈默.疲惫.png"
  },
  "scenes": {
    "长江大桥-栏杆": "assets/scenes/长江大桥-栏杆.png",
    "出租屋": "assets/scenes/出租屋.png"
  },
  "bgm": { "夜风": "assets/bgm/夜风.ogg" },
  "sfx": {},
  "cg": {}
}
```

- [ ] **Step 4: 运行 Python 测试（本环境验证）**

Run: `cd 99_game/tools && pip install -r requirements.txt && python -m pytest test_validate.py -v && python validate_chapter.py ../data/chapters/chapter01_新皮肤.json ../data/剧本.schema.json`
Expected: 3 个 pytest 通过，且 chapter01 输出 `OK: ... 通过 schema 校验`。

- [ ] **Step 5: 写 project.godot（最小可打开工程）**

Create `99_game/project.godot`:
```ini
config_version=5

[application]
config/name="他者之镜"
run/main_scene="res://scenes/Title.tscn"
config/features=PackedStringArray("4.3", "Forward Plus")

[autoload]
GameManager="*res://scripts/autoload/GameManager.gd"
ScriptInterpreter="*res://scripts/autoload/ScriptInterpreter.gd"
AudioManager="*res://scripts/autoload/AudioManager.gd"
SaveManager="*res://scripts/autoload/SaveManager.gd"

[display]
window/size/viewport_width=1536
window/size/viewport_height=1024
window/stretch/mode="canvas_items"
window/stretch/aspect="expand"

[input]
advance={ "deadzone": 0.5, "events": [Object(InputEventMouseButton,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"button_mask":0,"position":Vector2(0,0),"global_position":Vector2(0,0),"factor":1.0,"button_index":1,"canceled":false,"pressed":false,"double_click":false,"button_mask":0),"Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"echo":false,"script":null,"keycode":32,"physical_keycode":0,"key_label":0,"unicode":32,"location":0,"echo":false),"Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"echo":false,"script":null,"keycode":4194309,"physical_keycode":0,"key_label":0,"unicode":0,"location":0,"echo":false)] }
menu={ "deadzone": 0.5, "events": [Object(InputEventMouseButton,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"button_mask":0,"position":Vector2(0,0),"global_position":Vector2(0,0),"factor":1.0,"button_index":2,"canceled":false,"pressed":false,"double_click":false,"button_mask":0),"Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"echo":false,"script":null,"keycode":4194305,"physical_keycode":0,"key_label":0,"unicode":0,"location":0,"echo":false)] }
quick_save={ "deadzone": 0.5, "events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"echo":false,"script":null,"keycode":4194340,"physical_keycode":0,"key_label":0,"unicode":0,"location":0,"echo":false)] }
quick_load={ "deadzone": 0.5, "events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"echo":false,"script":null,"keycode":4194344,"physical_keycode":0,"key_label":0,"unicode":0,"location":0,"echo":false)] }
log={ "deadzone": 0.5, "events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"echo":false,"script":null,"keycode":72,"physical_keycode":0,"key_label":0,"unicode":104,"location":0,"echo":false)] }
auto_play={ "deadzone": 0.5, "events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"echo":false,"script":null,"keycode":65,"physical_keycode":0,"key_label":0,"unicode":97,"location":0,"echo":false)] }
skip={ "deadzone": 0.5, "events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"echo":false,"script":null,"keycode":83,"physical_keycode":0,"key_label":0,"unicode":115,"location":0,"echo":false)] }
```
> 上述 input action：advance=鼠标左键/空格/回车；menu=右键/ESC；F5=quick_save；F9=quick_load；H=log；A=auto_play；S=skip。Ctrl 快进用 `Input.is_key_pressed(KEY_CTRL)` 在代码里判定（无需 action）。

- [ ] **Step 6: 写 README**

Create `99_game/README.md`:
```markdown
# 他者之镜（Godot 工程）

## 打开
1. 安装 Godot 4.3+。
2. 用 Godot 导入本目录（`project.godot`）。

## 运行
F5 从标题开始 → 「开始游戏」进入 `chapter01_新皮肤.json`。

## 验收点
1. 桥上：陈默.沉重(center) 立绘 + 打字机台词。
2. choice：「再想想」→ 经 label keepgoing → 陈默.释然 → jump 到「回出租屋」段（自动换背景）。
3. choice：「跳下去」→ jump 到「结局_BE」段 → ending(BE) 进结局画面。
4. 立绘累积：narrate 期间立绘维持。
5. 段尾无 jump/ending 时回标题并提示「章节结束」。

## 数据校验（本环境可跑，无需 Godot）
cd tools && pip install -r requirements.txt
python validate_chapter.py ../data/chapters/chapter01_新皮肤.json ../data/剧本.schema.json
python -m pytest test_validate.py -v

## GUT 单测（需 Godot）
装 GUT 到 addons/gut/，命令行运行：
godot --headless -s addons/gut/gut_cmdln.gd -gdir=res://tests -gexit

## 资源
全部程序占位图，首次引用时生成到 assets/。换真图只改 data/manifest.json。
```

- [ ] **Step 7: 提交**

```bash
cd 99_game
git add project.godot README.md data/ tools/
git commit -m "feat(game): 工程骨架 + 示例剧本 + schema 校验"
```

---

## Task 2: Manifest 加载器

**Files:**
- Create: `99_game/scripts/data/Manifest.gd`
- Create: `99_game/tests/test_manifest.gd`

**Interfaces:**
- Consumes: `res://data/manifest.json`
- Produces: `Manifest`（autoload 注册名 `Manifest`），方法 `get_portrait/get_scene/get_bgm/get_sfx/get_cg(name) -> String`

- [ ] **Step 1: 写失败测试（GUT）**

Create `99_game/tests/test_manifest.gd`:
```gdscript
extends GutTest

var Manifest = preload("res://scripts/data/Manifest.gd")

func test_known_names_resolve():
    var m = Manifest.new()
    m.load_from_path("res://data/manifest.json")
    assert_eq(m.get_portrait("陈默.沉重"), "assets/portraits/陈默.沉重.png")
    assert_eq(m.get_scene("长江大桥-栏杆"), "assets/scenes/长江大桥-栏杆.png")
    assert_eq(m.get_bgm("夜风"), "assets/bgm/夜风.ogg")

func test_unknown_returns_empty():
    var m = Manifest.new()
    m.load_from_path("res://data/manifest.json")
    assert_eq(m.get_portrait("不存在"), "")
    assert_eq(m.get_scene("不存在"), "")
```

> 把 `Manifest.gd` 头部 `class_name Manifest` 去掉（autoload 名即类名）。为可在测试里 `preload` + `new`，脚本写成普通 Resource 风格，不依赖 autoload 单例化。

- [ ] **Step 2: 运行测试确认失败**

Run（需 Godot + GUT）: `godot --headless -s addons/gut/gut_cmdln.gd -gselect=test_manifest -gexit`
Expected: FAIL（`Manifest.gd` 不存在 / preload 失败）。

- [ ] **Step 3: 实现 Manifest**

Create `99_game/scripts/data/Manifest.gd`:
```gdscript
extends Node
## 逻辑名 → 资源路径 映射加载器。

var _data: Dictionary = {
    "portraits": {}, "scenes": {}, "bgm": {}, "sfx": {}, "cg": {}
}

func load_from_path(path: String) -> void:
    if not FileAccess.file_exists(path):
        push_error("Manifest: 文件不存在 %s" % path)
        return
    var text := FileAccess.get_file_as_string(path)
    var parsed = JSON.parse_string(text)
    if parsed == null:
        push_error("Manifest: JSON 解析失败 %s" % path)
        return
    for key in _data.keys():
        if parsed.has(key) and parsed[key] is Dictionary:
            _data[key] = parsed[key]

func get_portrait(name: String) -> String:
    return _data["portraits"].get(name, "")

func get_scene(name: String) -> String:
    return _data["scenes"].get(name, "")

func get_bgm(track: String) -> String:
    return _data["bgm"].get(track, "")

func get_sfx(track: String) -> String:
    return _data["sfx"].get(track, "")

func get_cg(name: String) -> String:
    return _data["cg"].get(name, "")
```

- [ ] **Step 4: 运行测试确认通过**

Run: `godot --headless -s addons/gut/gut_cmdln.gd -gselect=test_manifest -gexit`
Expected: PASS（2 用例）。

- [ ] **Step 5: 提交**

```bash
cd 99_game
git add scripts/data/Manifest.gd tests/test_manifest.gd
git commit -m "feat(data): Manifest 逻辑名→资源路径加载器"
```

---

## Task 3: ChapterLoader（加载/缓存/校验）

**Files:**
- Create: `99_game/scripts/data/ChapterLoader.gd`
- Create: `99_game/tests/test_chapter_loader.gd`

**Interfaces:**
- Consumes: `res://data/chapters/<file>.json`
- Produces: `ChapterLoader.load_chapter(file_name) -> Dictionary`；`file_name` 不带扩展名与 `res://data/chapters/` 前缀。返回完整章节 dict（`meta` + `scenes`）；失败返回 `{}`。

- [ ] **Step 1: 写失败测试**

Create `99_game/tests/test_chapter_loader.gd`:
```gdscript
extends GutTest

var ChapterLoader = preload("res://scripts/data/ChapterLoader.gd")

func test_load_known_chapter():
    var cl = ChapterLoader.new()
    var ch = cl.load_chapter("chapter01_新皮肤")
    assert_not_null(ch)
    assert_eq(ch.get("meta", {}).get("title"), "新皮肤·Day0")
    assert_true(ch.has("scenes"))
    assert_eq(len(ch["scenes"]), 3)

func test_missing_file_returns_empty():
    var cl = ChapterLoader.new()
    var ch = cl.load_chapter("不存在")
    assert_eq(ch, {})

func test_cached_on_second_call():
    var cl = ChapterLoader.new()
    var a = cl.load_chapter("chapter01_新皮肤")
    var b = cl.load_chapter("chapter01_新皮肤")
    assert_true(a == b, "同一引用（缓存）")
```

- [ ] **Step 2: 运行确认失败**

Run: `godot --headless -s addons/gut/gut_cmdln.gd -gselect=test_chapter_loader -gexit`
Expected: FAIL（脚本不存在）。

- [ ] **Step 3: 实现 ChapterLoader**

Create `99_game/scripts/data/ChapterLoader.gd`:
```gdscript
extends Node
## 章节文件加载/缓存/结构校验。file_name 不带扩展名。

const DIR := "res://data/chapters/"
var _cache: Dictionary = {}

func load_chapter(file_name: String) -> Dictionary:
    if _cache.has(file_name):
        return _cache[file_name]
    var path := DIR + file_name + ".json"
    if not FileAccess.file_exists(path):
        push_error("ChapterLoader: 文件不存在 %s" % path)
        return {}
    var parsed = JSON.parse_string(FileAccess.get_file_as_string(path))
    if parsed == null or not (parsed is Dictionary):
        push_error("ChapterLoader: JSON 解析失败 %s" % path)
        return {}
    if not _validate(parsed):
        push_error("ChapterLoader: 结构校验失败 %s" % path)
        return {}
    _cache[file_name] = parsed
    return parsed

func _validate(ch: Dictionary) -> bool:
    if not ch.has("meta") or not ch.has("scenes"):
        return false
    if not (ch["scenes"] is Array) or ch["scenes"].is_empty():
        return false
    for blk in ch["scenes"]:
        if not (blk is Dictionary):
            return false
        if not blk.has("id") or not blk.has("scene") or not blk.has("lines"):
            return false
        if not (blk["lines"] is Array):
            return false
        for line in blk["lines"]:
            if not (line is Dictionary) or not line.has("op"):
                return false
    return true
```

- [ ] **Step 4: 运行确认通过**

Run: `godot --headless -s addons/gut/gut_cmdln.gd -gselect=test_chapter_loader -gexit`
Expected: PASS（3 用例）。

- [ ] **Step 5: 提交**

```bash
cd 99_game
git add scripts/data/ChapterLoader.gd tests/test_chapter_loader.gd
git commit -m "feat(data): ChapterLoader 加载/缓存/结构校验"
```

---

## Task 4: ScriptInterpreter —— 执行循环与指令分发

**Files:**
- Create: `99_game/scripts/autoload/ScriptInterpreter.gd`
- Create: `99_game/tests/test_interpreter_exec.gd`

**Interfaces:**
- Consumes: `ChapterLoader.load_chapter`
- Produces: autoload `ScriptInterpreter`；公开 `start(file, scene_id="", label="")`、`advance()`；信号见 File Structure。内部状态 `_file/_scenes/_scene_idx/_line_idx`。

> 本任务覆盖：start 定位、即时指令自动递进、阻塞指令停指针、段尾 chapter_finished、ending 信号。跳转与立绘槽在下两个任务补全（本任务的 jump 暂调用 `resolve_target` 占位→下一任务实现；本任务先保证不崩）。

- [ ] **Step 1: 写失败测试**

Create `99_game/tests/test_interpreter_exec.gd`:
```gdscript
extends GutTest

func before_each():
    # Manifest autoload 需先加载，解释器依赖它（占位图路径解析在 PortraitLayer，这里不触发）
    if not Engine.has_singleton("Manifest"):
        var m = preload("res://scripts/data/Manifest.gd").new()
        m.load_from_path("res://data/manifest.json")
        Engine.register_singleton("Manifest", m)

func test_narrate_then_say_emits_lines():
    var interp = preload("res://scripts/autoload/ScriptInterpreter.gd").new()
    var got := []
    interp.line_ready.connect(func(kind, payload): got.append([kind, payload]))
    interp.start("chapter01_新皮肤", "桥上")
    # 第一条是 say（阻塞），advance 前 line_ready 已发出 say
    assert_eq(got[0][0], "say")
    assert_eq(got[0][1]["text"], "就到这里吧。")

func test_advance_moves_past_blocking():
    var interp = preload("res://scripts/autoload/ScriptInterpreter.gd").new()
    var got := []
    interp.line_ready.connect(func(kind, payload):
        got.append([kind, payload]))
    interp.start("chapter01_新皮肤", "桥上")
    got.clear()
    interp.advance()  # 离开 say，下一条是 narrate（阻塞）→ 发 narrate 后停
    assert_eq(got[-1][0], "narrate")
    assert_eq(got[-1][1]["text"], "江风很大。他翻过栏杆。")

func test_scene_block_bg_emitted_on_enter():
    var interp = preload("res://scripts/autoload/ScriptInterpreter.gd").new()
    var bg := ""
    interp.bg_changed.connect(func(scene_name, time): bg = scene_name)
    interp.start("chapter01_新皮肤", "桥上")
    assert_eq(bg, "长江大桥-栏杆")
```

- [ ] **Step 2: 运行确认失败**

Run: `godot --headless -s addons/gut/gut_cmdln.gd -gselect=test_interpreter_exec -gexit`
Expected: FAIL（脚本不存在）。

- [ ] **Step 3: 实现 ScriptInterpreter（执行循环）**

Create `99_game/scripts/autoload/ScriptInterpreter.gd`:
```gdscript
extends Node
## 集中式剧本解释器。持有运行时状态，match op 分发指令。

signal line_ready(kind: String, payload: Dictionary)
signal portrait_changed(slots: Dictionary)
signal bg_changed(scene_name: String, time: String)
signal bgm_changed(track: String, mode: String, loop: bool)
signal choice_presented(options: Array)
signal ended(kind: String, title: String, cg: String)
signal chapter_finished()

var _file: String = ""
var _chapter: Dictionary = {}
var _scenes: Array = []
var _scene_idx: int = 0
var _line_idx: int = 0

var _ChapterLoader = preload("res://scripts/data/ChapterLoader.gd")
var _loader

func _ready() -> void:
    _loader = _ChapterLoader.new()

func start(file: String, scene_id: String = "", label: String = "") -> void:
    _file = file
    _chapter = _loader.load_chapter(file)
    if _chapter.is_empty():
        push_error("ScriptInterpreter: 无法加载章节 %s" % file)
        chapter_finished.emit()
        return
    _scenes = _chapter["scenes"]
    _scene_idx = 0
    if scene_id != "":
        _scene_idx = _find_scene_index(scene_id)
    _enter_scene_block()
    if label != "":
        _line_idx = _find_label(label)
    _run_from_current()

func advance() -> void:
    _line_idx += 1
    _run_from_current()

# 内部：进入场景段时套用 scene + bgm，从 line 0 起
func _enter_scene_block() -> void:
    _line_idx = 0
    var blk: Dictionary = _scenes[_scene_idx]
    bg_changed.emit(blk.get("scene", ""), blk.get("time", ""))
    if blk.has("bgm"):
        var b: Dictionary = blk["bgm"]
        bgm_changed.emit(b.get("track", ""), b.get("mode", "play"), b.get("loop", true))

# 从当前 line 起推进：即时指令自动递进，阻塞/终止停下
func _run_from_current() -> void:
    while true:
        if _scene_idx >= len(_scenes):
            chapter_finished.emit()
            return
        var blk: Dictionary = _scenes[_scene_idx]
        var lines: Array = blk["lines"]
        if _line_idx >= len(lines):
            # 当前段执行完且无 jump/ending：章节结束
            chapter_finished.emit()
            return
        var line: Dictionary = lines[_line_idx]
        var op: String = line["op"]
        match op:
            "say":
                line_ready.emit("say", line)
                return  # 阻塞
            "narrate":
                line_ready.emit("narrate", line)
                return  # 阻塞
            "choice":
                choice_presented.emit(line["options"])
                return  # 阻塞
            "ending":
                ended.emit(line.get("kind", "NE"), line.get("title", ""), line.get("cg", ""))
                return  # 终止
            "label":
                _line_idx += 1
                continue
            "show", "hide":
                _apply_portrait(line)
                _line_idx += 1
                continue
            "bg":
                bg_changed.emit(line.get("scene", ""), line.get("time", ""))
                _line_idx += 1
                continue
            "bgm":
                bgm_changed.emit(line.get("track", ""), line.get("mode", "play"), line.get("loop", true))
                _line_idx += 1
                continue
            "sfx":
                _line_idx += 1
                continue
            "jump":
                _do_jump(line)
                continue  # _do_jump 已重定位 _scene_idx/_line_idx，继续推进
            _:
                push_error("ScriptInterpreter: 未知 op %s" % op)
                _line_idx += 1

# 立绘累积：show/hide 改槽（say 的立绘在 PortraitLayer 渲染时读 line，槽状态由 show/hide 维护）
# 注：say 的 who+portrait+pos 也维护槽，见 Task 6
func _apply_portrait(line: Dictionary) -> void:
    # 占位：实际累积逻辑在 Task 6 替换
    pass

func _do_jump(line: Dictionary) -> void:
    # 占位：Task 5 实现 resolve_target
    pass

func _find_scene_index(scene_id: String) -> int:
    for i in len(_scenes):
        if _scenes[i].get("id", "") == scene_id:
            return i
    push_error("ScriptInterpreter: 找不到场景段 %s" % scene_id)
    return 0

func _find_label(name: String) -> int:
    var lines: Array = _scenes[_scene_idx]["lines"]
    for i in len(lines):
        if lines[i].get("op") == "label" and lines[i].get("name") == name:
            return i
    push_error("ScriptInterpreter: 找不到 label %s" % name)
    return 0
```

- [ ] **Step 4: 运行确认通过**

Run: `godot --headless -s addons/gut/gut_cmdln.gd -gselect=test_interpreter_exec -gexit`
Expected: PASS（3 用例）。

- [ ] **Step 5: 提交**

```bash
cd 99_game
git add scripts/autoload/ScriptInterpreter.gd tests/test_interpreter_exec.gd
git commit -m "feat(interp): 解释器执行循环与指令分发"
```

---

## Task 5: ScriptInterpreter —— 跳转寻址 resolve_target

**Files:**
- Modify: `99_game/scripts/autoload/ScriptInterpreter.gd`（替换 `_do_jump`，新增 `resolve_target`）
- Modify: `99_game/scripts/autoload/ScriptInterpreter.gd`（`choice_presented` 后由 UI 调 `choose(option)`）
- Create: `99_game/tests/test_interpreter_jump.gd`

**Interfaces:**
- Produces: `ScriptInterpreter.choose(option: Dictionary) -> void`（玩家选定选项后调用，option 含 `to/scene/file`），`ScriptInterpreter.resolve_target(to, scene, file) -> bool`（重定位指针，成功 true）。

- [ ] **Step 1: 写失败测试**

Create `99_game/tests/test_interpreter_jump.gd`:
```gdscript
extends GutTest

func _new_interp():
    var interp = preload("res://scripts/autoload/ScriptInterpreter.gd").new()
    return interp

func test_jump_to_scene_block():
    var interp = _new_interp()
    var last_bg := ""
    interp.bg_changed.connect(func(s, t): last_bg = s)
    interp.start("chapter01_新皮肤", "桥上")
    interp._do_jump({"op": "jump", "scene": "回出租屋"})
    assert_eq(last_bg, "出租屋", "jump 到段应套用其 scene")

func test_resolve_label_within_block():
    var interp = _new_interp()
    interp.start("chapter01_新皮肤", "桥上")
    # 推进到 choice 后选「再想想」→ to=keepgoing
    var ok := interp.resolve_target("keepgoing", "", "")
    assert_true(ok)
    # 下一行应是 label 后的 say（释然）
    var got := []
    interp.line_ready.connect(func(k, p): got.append(p))
    interp._run_from_current()
    assert_eq(got[0].get("portrait"), "释然")

func test_choose_drives_jump_to_ending_scene():
    var interp = _new_interp()
    var ended_kind := ""
    interp.ended.connect(func(k, t, c): ended_kind = k)
    interp.start("chapter01_新皮肤", "桥上")
    interp.advance()  # 过 say
    interp.advance()  # 过 narrate → 撞 choice 停住（choice_presented 已发）
    interp.choose({"label": "跳下去", "scene": "结局_BE", "leads_to_ending": true})
    # 结局段：narrate（阻塞，停在 advance 前）→ 需 advance 过 narrate 到 ending
    interp.advance()
    assert_eq(ended_kind, "BE")

func test_missing_target_returns_false():
    var interp = _new_interp()
    interp.start("chapter01_新皮肤", "桥上")
    var ok := interp.resolve_target("不存在的label", "", "")
    assert_false(ok)
```

- [ ] **Step 2: 运行确认失败**

Run: `godot --headless -s addons/gut/gut_cmdln.gd -gselect=test_interpreter_jump -gexit`
Expected: FAIL（`_do_jump` 是空 pass / `choose` 不存在）。

- [ ] **Step 3: 实现跳转寻址与 choose**

在 `ScriptInterpreter.gd` 中替换 `_do_jump` 为：
```gdscript
func _do_jump(line: Dictionary) -> void:
    resolve_target(line.get("to", ""), line.get("scene", ""), line.get("file", ""))

func choose(option: Dictionary) -> void:
    if not resolve_target(option.get("to", ""), option.get("scene", ""), option.get("file", "")):
        push_error("ScriptInterpreter: 选项目标无法定位 %s" % str(option))
        chapter_finished.emit()
        return
    _run_from_current()

# 重定位指令指针。返回 false 表示定位失败。
func resolve_target(to: String, scene: String, file: String) -> bool:
    # file：跨章节
    if file != "":
        var new_chapter := _loader.load_chapter(file)
        if new_chapter.is_empty():
            push_error("ScriptInterpreter: 跨章节文件不存在 %s" % file)
            return false
        _file = file
        _chapter = new_chapter
        _scenes = new_chapter["scenes"]
        _scene_idx = 0
    # scene：本/新章节内定位段
    if scene != "":
        _scene_idx = _find_scene_index(scene)
        _enter_scene_block()
    # to：段内 label
    if to != "":
        _line_idx = _find_label(to)
    return true
```

- [ ] **Step 4: 运行确认通过**

Run: `godot --headless -s addons/gut/gut_cmdln.gd -gselect=test_interpreter_jump -gexit`
Expected: PASS（4 用例）。

- [ ] **Step 5: 提交**

```bash
cd 99_game
git add scripts/autoload/ScriptInterpreter.gd tests/test_interpreter_jump.gd
git commit -m "feat(interp): 跳转寻址 resolve_target 与 choose"
```

---

## Task 6: ScriptInterpreter —— 立绘槽累积

**Files:**
- Modify: `99_game/scripts/autoload/ScriptInterpreter.gd`（替换 `_apply_portrait`，新增槽状态，say 也维护槽）
- Create: `99_game/tests/test_interpreter_portraits.gd`

**Interfaces:**
- Produces: `ScriptInterpreter.slots: Dictionary = {left=null, center=null, right=null}`（值为 `{who, portrait}` 或 null）；`portrait_changed` 在 say/show/hide 后发出全槽快照。

- [ ] **Step 1: 写失败测试**

Create `99_game/tests/test_interpreter_portraits.gd`:
```gdscript
extends GutTest

func _new_interp():
    return preload("res://scripts/autoload/ScriptInterpreter.gd").new()

func test_say_sets_slot():
    var interp = _new_interp()
    var snap: Dictionary = {}
    interp.portrait_changed.connect(func(s): snap = s)
    interp.start("chapter01_新皮肤", "桥上")  # 首条 say: 陈默.沉重 center
    assert_eq(snap["center"]["who"], "陈默")
    assert_eq(snap["center"]["portrait"], "沉重")
    assert_is_null(snap["left"])

func test_show_hide_updates_slots():
    var interp = _new_interp()
    var snap: Dictionary = {}
    interp.portrait_changed.connect(func(s): snap = s.duplicate(true))
    interp._apply_portrait({"op": "show", "who": "陈默", "portrait": "疲惫", "pos": "left"})
    assert_eq(snap["left"]["portrait"], "疲惫")
    interp._apply_portrait({"op": "hide", "who": "陈默"})
    assert_is_null(snap["left"])

func test_narrate_keeps_slots():
    var interp = _new_interp()
    interp.start("chapter01_新皮肤", "桥上")  # say 设 center
    var snap: Dictionary = {}
    interp.portrait_changed.connect(func(s): snap = s.duplicate(true))
    interp.advance()  # 到 narrate，narrate 不改槽，但前一条 say 已设过
    assert_eq(snap["center"]["portrait"], "沉重")
```

- [ ] **Step 2: 运行确认失败**

Run: `godot --headless -s addons/gut/gut_cmdln.gd -gselect=test_interpreter_portraits -gexit`
Expected: FAIL（槽未被维护）。

- [ ] **Step 3: 实现立绘槽**

在 `ScriptInterpreter.gd` 顶部状态区新增：
```gdscript
var slots: Dictionary = {"left": null, "center": null, "right": null}
```

替换 `_apply_portrait` 并让 say 也维护槽。修改 `_run_from_current` 的 `"say"` 分支为：
```gdscript
"say":
    _set_slot(line.get("pos", "center"), line.get("who", ""), line.get("portrait", ""))
    portrait_changed.emit(_snapshot())
    line_ready.emit("say", line)
    return
```

替换 `_apply_portrait`：
```gdscript
func _apply_portrait(line: Dictionary) -> void:
    var op: String = line["op"]
    if op == "show":
        _set_slot(line.get("pos", "center"), line.get("who", ""), line.get("portrait", ""))
        portrait_changed.emit(_snapshot())
    elif op == "hide":
        _clear_slot_by_who(line.get("who", ""))
        portrait_changed.emit(_snapshot())

func _set_slot(pos: String, who: String, portrait: String) -> void:
    if slots.has(pos):
        slots[pos] = {"who": who, "portrait": portrait}

func _clear_slot_by_who(who: String) -> void:
    for k in slots.keys():
        if slots[k] != null and slots[k].get("who", "") == who:
            slots[k] = null

func _snapshot() -> Dictionary:
    return slots.duplicate(true)
```

并在 `start()` 开头重置槽：`slots = {"left": null, "center": null, "right": null}`。

- [ ] **Step 4: 运行确认通过**

Run: `godot --headless -s addons/gut/gut_cmdln.gd -gselect=test_interpreter_portraits -gexit`
Expected: PASS（3 用例）。

- [ ] **Step 5: 提交**

```bash
cd 99_game
git add scripts/autoload/ScriptInterpreter.gd tests/test_interpreter_portraits.gd
git commit -m "feat(interp): 立绘槽累积维护"
```

---

## Task 7: PlaceholderGen 占位图生成

**Files:**
- Create: `99_game/scripts/util/PlaceholderGen.gd`
- Create: `99_game/tests/test_placeholder_gen.gd`

**Interfaces:**
- Produces: `PlaceholderGen.get_portrait_image(who: String, portrait: String) -> ImageTexture`；`get_scene_image(scene: String) -> ImageTexture`；`get_avatar_image(who: String) -> ImageTexture`。首次生成缓存到 `res://assets/<...>.png`（运行时 `user://` 更安全，但 demo 用 res + 首次写入需 `ResourceSaver`；这里改为内存生成 + 磁盘缓存到 `user://placeholder_cache/`）。

> 决策：占位图内存生成，按需缓存到 `user://placeholder_cache/`（避免 res:// 只读问题）。

- [ ] **Step 1: 写失败测试**

Create `99_game/tests/test_placeholder_gen.gd`:
```gdscript
extends GutTest

var PlaceholderGen = preload("res://scripts/util/PlaceholderGen.gd")

func test_portrait_image_size():
    var g = PlaceholderGen.new()
    var tex = g.get_portrait_image("陈默", "沉重")
    assert_eq(tex.get_width(), 400)
    assert_eq(tex.get_height(), 800)

func test_scene_image_size():
    var g = PlaceholderGen.new()
    var tex = g.get_scene_image("长江大桥-栏杆")
    assert_eq(tex.get_width(), 1536)
    assert_eq(tex.get_height(), 1024)
```

- [ ] **Step 2: 运行确认失败**

Run: `godot --headless -s addons/gut/gut_cmdln.gd -gselect=test_placeholder_gen -gexit`
Expected: FAIL。

- [ ] **Step 3: 实现 PlaceholderGen**

Create `99_game/scripts/util/PlaceholderGen.gd`:
```gdscript
extends Node
## 程序占位图：立绘 400×800 纯绿底、场景 1536×1024 渐变、头像方形。

const CHROMA := Color(0.0, 1.0, 0.0)  # #00FF00
const CACHE_DIR := "user://placeholder_cache/"

func get_portrait_image(who: String, portrait: String) -> ImageTexture:
    var key := "%s.%s" % [who, portrait]
    var path := CACHE_DIR + "portrait_" + _safe(key) + ".png"
    var img := _load_or_make(path, func(): return _make_portrait(key))
    return ImageTexture.create_from_image(img)

func get_scene_image(scene: String) -> ImageTexture:
    var path := CACHE_DIR + "scene_" + _safe(scene) + ".png"
    var img := _load_or_make(path, func(): return _make_scene(scene))
    return ImageTexture.create_from_image(img)

func get_avatar_image(who: String) -> ImageTexture:
    var path := CACHE_DIR + "avatar_" + _safe(who) + ".png"
    var img := _load_or_make(path, func(): return _make_avatar(who))
    return ImageTexture.create_from_image(img)

func _load_or_make(path: String, maker: Callable) -> Image:
    if FileAccess.file_exists(path):
        var cached := Image.load_from_file(path)
        if cached != null:
            return cached
    DirAccess.make_dir_recursive_absolute(CACHE_DIR)
    var img: Image = maker.call()
    img.save_png(path)
    return img

func _make_portrait(label: String) -> Image:
    var img := Image.create(400, 800, false, Image.FORMAT_RGBA8)
    img.fill(CHROMA)
    _draw_label(img, label, 400, 800)
    return img

func _make_scene(label: String) -> Image:
    var img := Image.create(1536, 1024, false, Image.FORMAT_RGBA8)
    for y in 1024:
        var t := float(y) / 1024.0
        img.fill_rect(Rect2i(0, y, 1536, 1), Color(0.15 + t*0.2, 0.15, 0.25, 1.0))
    _draw_label(img, "场景：" + label, 1536, 1024)
    return img

func _make_avatar(label: String) -> Image:
    var img := Image.create(128, 128, false, Image.FORMAT_RGBA8)
    img.fill(Color(0.3, 0.3, 0.35, 1.0))
    _draw_label(img, label, 128, 128)
    return img

# 简易文字：用大像素块拼不现实，这里用占位色块 + 不依赖字体的标注。
# 真实文字渲染由 PortraitLayer/DialogueBox 用 Label 节点叠加；图片本身只填色。
func _draw_label(img: Image, label: String, w: int, h: int) -> void:
    img.fill_rect(Rect2i(w/4, h/2 - 20, w/2, 40), Color(0.0, 0.0, 0.0, 0.4))

func _safe(s: String) -> String:
    return s.replace(".", "_").replace("/", "_").replace(":", "_")
```

> 说明：图片内不内嵌中文文字（Godot 无字体 Image 绘制不便）；角色名/逻辑名由 UI 层的 `Label` 叠加显示。占位图仅用颜色区分（立绘纯绿、场景渐变），符合 chroma key 用途。

- [ ] **Step 4: 运行确认通过**

Run: `godot --headless -s addons/gut/gut_cmdln.gd -gselect=test_placeholder_gen -gexit`
Expected: PASS（2 用例）。

- [ ] **Step 5: 提交**

```bash
cd 99_game
git add scripts/util/PlaceholderGen.gd tests/test_placeholder_gen.gd
git commit -m "feat(util): 程序占位图生成与缓存"
```

---

## Task 8: AudioManager

**Files:**
- Create: `99_game/scripts/autoload/AudioManager.gd`

**Interfaces:**
- Consumes: `Manifest.get_bgm/get_sfx`
- Produces: autoload `AudioManager`；`play_bgm(track, loop=true)`、`stop_bgm()`、`fade_bgm(track, loop=true)`、`play_sfx(track)`、音量设置 `set_volume(channel, value)`。资源缺失静默跳过。

- [ ] **Step 1: 实现（无复杂逻辑，单测以手动验收为主）**

Create `99_game/scripts/autoload/AudioManager.gd`:
```gdscript
extends Node

var _bgm: AudioStreamPlayer = null
var _sfx_player: AudioStreamPlayer = null
var _bgm_loop: bool = true

func _ready() -> void:
    _bgm = AudioStreamPlayer.new()
    _sfx_player = AudioStreamPlayer.new()
    add_child(_bgm)
    add_child(_sfx_player)

func play_bgm(track: String, loop: bool = true) -> void:
    var path := _manifest().get_bgm(track) if _manifest() else ""
    if path == "" or not ResourceLoader.exists(path):
        push_warning("AudioManager: BGM 资源缺失 %s" % track)
        return
    _bgm.stream = load(path)
    _bgm_loop = loop
    _bgm.play()

func stop_bgm() -> void:
    _bgm.stop()

func fade_bgm(track: String, loop: bool = true) -> void:
    # V1：简化为切换播放目标轨道（无真实淡入淡出）
    play_bgm(track, loop)

func play_sfx(track: String) -> void:
    var path := _manifest().get_sfx(track) if _manifest() else ""
    if path == "" or not ResourceLoader.exists(path):
        push_warning("AudioManager: SFX 资源缺失 %s" % track)
        return
    _sfx_player.stream = load(path)
    _sfx_player.play()

func set_volume(channel: String, value_db: float) -> void:
    match channel:
        "bgm": _bgm.volume_db = value_db
        "sfx": _sfx_player.volume_db = value_db

func _manifest():
    return Engine.get_singleton("Manifest") if Engine.has_singleton("Manifest") else null
```

> 注册到 autoload 的 AudioManager 会在主场景跑动时 `_ready()`。Demo 无真实音频文件，BGM/SFX 均走"缺失静默跳过"分支，不阻塞管线。

- [ ] **Step 2: 提交**

```bash
cd 99_game
git add scripts/autoload/AudioManager.gd
git commit -m "feat(audio): AudioManager bgm/sfx，缺失静默"
```

---

## Task 9: SaveManager

**Files:**
- Create: `99_game/scripts/autoload/SaveManager.gd`
- Create: `99_game/tests/test_save_manager.gd`

**Interfaces:**
- Consumes: `ScriptInterpreter` 状态（由 Game 场景传入快照）
- Produces: autoload `SaveManager`；`save_slot(name: String, snapshot: Dictionary)`、`load_slot(name: String) -> Dictionary`、`list_slots() -> Array`；快捷槽常量 `QUICK="quick"`、`AUTO="auto"`。快照结构 `{file, scene_id, line_idx, slots, bg, bgm}`。

- [ ] **Step 1: 写失败测试**

Create `99_game/tests/test_save_manager.gd`:
```gdscript
extends GutTest

var SaveManager = preload("res://scripts/autoload/SaveManager.gd")

func test_save_and_load_roundtrip():
    var sm = SaveManager.new()
    var snap := {"file": "chapter01_新皮肤", "scene_id": "桥上", "line_idx": 2,
                 "slots": {"left": null, "center": {"who": "陈默", "portrait": "沉重"}, "right": null},
                 "bg": "长江大桥-栏杆", "bgm": "夜风"}
    sm.save_slot("test_slot", snap)
    var loaded = sm.load_slot("test_slot")
    assert_eq(loaded["scene_id"], "桥上")
    assert_eq(loaded["line_idx"], 2)
    assert_eq(loaded["slots"]["center"]["portrait"], "沉重")

func test_list_slots_includes_saved():
    var sm = SaveManager.new()
    sm.save_slot("test_list", {"file": "x", "scene_id": "s", "line_idx": 0, "slots": {}, "bg": "", "bgm": ""})
    var slots = sm.list_slots()
    assert_true("test_list" in slots)
```

- [ ] **Step 2: 运行确认失败**

Run: `godot --headless -s addons/gut/gut_cmdln.gd -gselect=test_save_manager -gexit`
Expected: FAIL。

- [ ] **Step 3: 实现 SaveManager**

Create `99_game/scripts/autoload/SaveManager.gd`:
```gdscript
extends Node
## 存档：JSON 序列化到 user://saves/。V1 无变量无 flag，快照即指针+槽+bg+bgm。

const SAVE_DIR := "user://saves/"
const QUICK := "quick"
const AUTO := "auto"
const EXT := ".json"

func _ready() -> void:
    DirAccess.make_dir_recursive_absolute(SAVE_DIR)

func save_slot(slot_name: String, snapshot: Dictionary) -> void:
    var path := SAVE_DIR + slot_name + EXT
    var f := FileAccess.open(path, FileAccess.WRITE)
    if f == null:
        push_error("SaveManager: 无法写入 %s" % path)
        return
    f.store_string(JSON.stringify(snapshot, "  "))
    f.close()

func load_slot(slot_name: String) -> Dictionary:
    var path := SAVE_DIR + slot_name + EXT
    if not FileAccess.file_exists(path):
        return {}
    var parsed = JSON.parse_string(FileAccess.get_file_as_string(path))
    return parsed if parsed is Dictionary else {}

func list_slots() -> Array:
    var names: Array = []
    var dir := DirAccess.open(SAVE_DIR)
    if dir == null:
        return names
    dir.list_dir_begin()
    var fn := dir.get_next()
    while fn != "":
        if fn.ends_with(EXT):
            names.append(fn.get_basename())
        fn = dir.get_next()
    return names
```

- [ ] **Step 4: 运行确认通过**

Run: `godot --headless -s addons/gut/gut_cmdln.gd -gselect=test_save_manager -gexit`
Expected: PASS（2 用例）。

- [ ] **Step 5: 提交**

```bash
cd 99_game
git add scripts/autoload/SaveManager.gd tests/test_save_manager.gd
git commit -m "feat(save): SaveManager JSON 槽位存读"
```

---

## Task 10: DialogueBox（对话框 + 打字机）

**Files:**
- Create: `99_game/scripts/ui/DialogueBox.gd`

**Interfaces:**
- Consumes: `Manifest.get_avatar`（头像）、`PlaceholderGen.get_avatar_image`（头像缺失时占位）
- Produces: `DialogueBox`（Control 子类）；`show_line(who: String, portrait: String, text: String, is_narrate: bool)`、`is_typing() -> bool`、`finish_typing()`、信号 `finished_typing`。自身含头像/名称/正文/继续指示；底部 Auto/Skip/Log/Menu 按钮发各自信号。

- [ ] **Step 1: 实现 DialogueBox**

Create `99_game/scripts/ui/DialogueBox.gd`:
```gdscript
extends Control
## 底部对话框：头像/名称/打字机正文/继续指示 + Auto/Skip/Log/Menu。

signal finished_typing()
signal button_auto()
signal button_skip()
signal button_log()
signal button_menu()

const BOX_H := 240
var _avatar := TextureRect.new()
var _name := Label.new()
var _body := RichTextLabel.new()
var _continue := Label.new()
var _char_delay: float = 0.03  # 秒/字
var _full_text := ""
var _shown := 0
var _typing := false
var _timer := Timer.new()

func _ready() -> void:
    set_anchors_preset(Control.PRESET_FULL_RECT)
    var box := Panel.new()
    box.set_anchors_preset(Control.PRESET_BOTTOM_WIDE)
    box.offset_top = -BOX_H
    add_child(box)
    _avatar.custom_minimum_size = Vector2(128, 128)
    _avatar.position = Vector2(20, 20)
    _avatar.size = Vector2(128, 128)
    box.add_child(_avatar)
    _name.position = Vector2(168, 16)
    _name.add_theme_font_size_override("font_size", 28)
    box.add_child(_name)
    _body.position = Vector2(168, 56)
    _body.size = Vector2(1300, 150)
    _body.bbcode_enabled = true
    _body.add_theme_font_size_override("normal_font_size", 26)
    box.add_child(_body)
    _continue.text = "点击继续 ▼"
    _continue.position = Vector2(1280, 200)
    _continue.visible = false
    box.add_child(_continue)
    _make_buttons(box)
    add_child(_timer)
    _timer.one_shot = false
    _timer.timeout.connect(_tick)

func _make_buttons(box: Panel) -> void:
    var names := ["Auto", "Skip", "Log", "Menu"]
    var sigs := [button_auto, button_skip, button_log, button_menu]
    for i in names.size():
        var b := Button.new()
        b.text = names[i]
        b.position = Vector2(700 + i * 130, 16)
        b.size = Vector2(120, 36)
        b.pressed.connect(func(): sigs[i].emit())
        box.add_child(b)

func show_line(who: String, portrait: String, text: String, is_narrate: bool) -> void:
    _name.text = "" if is_narrate else who
    _avatar.texture = _resolve_avatar(who)
    _full_text = text
    _shown = 0
    _body.text = ""
    _continue.visible = false
    _typing = true
    _timer.start(_char_delay)

func is_typing() -> bool:
    return _typing

func finish_typing() -> void:
    _typing = false
    _timer.stop()
    _body.text = _full_text
    _shown = _full_text.length()
    _continue.visible = true
    finished_typing.emit()

func _tick() -> void:
    if not _typing:
        return
    _shown += 1
    _body.text = _full_text.substr(0, _shown)
    if _shown >= _full_text.length():
        finish_typing()

func _resolve_avatar(who: String) -> Texture2D:
    var man = Engine.get_singleton("Manifest") if Engine.has_singleton("Manifest") else null
    var path := man.get_portrait(who + ".default") if man else ""  # 头像无独立逻辑名，退化为占位
    if path != "" and ResourceLoader.exists(path):
        return load(path)
    var pg = preload("res://scripts/util/PlaceholderGen.gd").new()
    return pg.get_avatar_image(who)
```

> 说明：头像逻辑名暂用 `<who>.default`，manifest 未提供则走占位。打字机点击跳过由 Game 场景在 advance 输入时调 `finish_typing()`。

- [ ] **Step 2: 提交**

```bash
cd 99_game
git add scripts/ui/DialogueBox.gd
git commit -m "feat(ui): DialogueBox 打字机对话框"
```

---

## Task 11: PortraitLayer（立绘累积渲染）

**Files:**
- Create: `99_game/scripts/ui/PortraitLayer.gd`

**Interfaces:**
- Consumes: `Manifest.get_portrait`、`PlaceholderGen.get_portrait_image`、解释器 `portrait_changed(slots)`
- Produces: `PortraitLayer`（Control 子类）；`apply_slots(slots: Dictionary)`。左中右三槽，center 居中、left/right 分列；缺失立绘走占位。

- [ ] **Step 1: 实现 PortraitLayer**

Create `99_game/scripts/ui/PortraitLayer.gd`:
```gdscript
extends Control
## 左/中/右立绘累积渲染。

const PW := 400
const PH := 800
var _nodes: Dictionary = {"left": null, "center": null, "right": null}

func _ready() -> void:
    set_anchors_preset(Control.PRESET_FULL_RECT)
    for k in _nodes.keys():
        var tr := TextureRect.new()
        tr.custom_minimum_size = Vector2(PW, PH)
        tr.size = Vector2(PW, PH)
        _position(tr, k)
        tr.visible = false
        add_child(tr)
        _nodes[k] = tr

func _position(tr: TextureRect, key: String) -> void:
    var screen_w := 1536.0
    match key:
        "left": tr.position = Vector2(120, 120)
        "center": tr.position = Vector2((screen_w - PW) / 2.0, 120)
        "right": tr.position = Vector2(screen_w - PW - 120, 120)

func apply_slots(slots: Dictionary) -> void:
    for k in _nodes.keys():
        var tr: TextureRect = _nodes[k]
        var v = slots.get(k)
        if v == null:
            tr.visible = false
            tr.texture = null
        else:
            tr.texture = _resolve(v["who"], v["portrait"])
            tr.visible = true

func _resolve(who: String, portrait: String) -> Texture2D:
    var logical := "%s.%s" % [who, portrait]
    var man = Engine.get_singleton("Manifest") if Engine.has_singleton("Manifest") else null
    var path := man.get_portrait(logical) if man else ""
    if path != "" and ResourceLoader.exists(path):
        return load(path)
    var pg = preload("res://scripts/util/PlaceholderGen.gd").new()
    return pg.get_portrait_image(who, portrait)
```

- [ ] **Step 2: 提交**

```bash
cd 99_game
git add scripts/ui/PortraitLayer.gd
git commit -m "feat(ui): PortraitLayer 立绘累积渲染"
```

---

## Task 12: ChoiceMenu + Backlog

**Files:**
- Create: `99_game/scripts/ui/ChoiceMenu.gd`
- Create: `99_game/scripts/ui/Backlog.gd`

**Interfaces:**
- ChoiceMenu：信号 `option_chosen(option: Dictionary)`；`show_options(options: Array)`。
- Backlog：`append(who: String, text: String)`、`toggle()`。

- [ ] **Step 1: 实现 ChoiceMenu**

Create `99_game/scripts/ui/ChoiceMenu.gd`:
```gdscript
extends Control
## 选项面板。

signal option_chosen(option: Dictionary)
var _container := VBoxContainer.new()

func _ready() -> void:
    set_anchors_preset(Control.PRESET_FULL_RECT)
    _container.set_anchors_preset(Control.PRESET_CENTER)
    _container.position = Vector2(-150, -120)
    add_child(_container)
    hide()

func show_options(options: Array) -> void:
    for c in _container.get_children():
        c.queue_free()
    for opt in options:
        var b := Button.new()
        b.text = opt["label"]
        b.custom_minimum_size = Vector2(300, 56)
        b.pressed.connect(func(): _on_chosen(opt))
        _container.add_child(b)
    show()

func _on_chosen(opt: Dictionary) -> void:
    hide()
    option_chosen.emit(opt)
```

- [ ] **Step 2: 实现 Backlog**

Create `99_game/scripts/ui/Backlog.gd`:
```gdscript
extends Control
## 对话历史 Log。

var _panel := Panel.new()
var _label := RichTextLabel.new()
var _entries: Array = []

func _ready() -> void:
    set_anchors_preset(Control.PRESET_FULL_RECT)
    _panel.set_anchors_preset(Control.PRESET_FULL_RECT)
    _panel.offset_left = 100; _panel.offset_right = -100
    _panel.offset_top = 80; _panel.offset_bottom = -80
    add_child(_panel)
    _label.set_anchors_preset(Control.PRESET_FULL_RECT)
    _label.offset_left = 20; _label.offset_right = -20
    _label.offset_top = 20; _label.offset_bottom = -20
    _label.bbcode_enabled = true
    _label.scroll_following = true
    _panel.add_child(_label)
    hide()

func append(who: String, text: String) -> void:
    var line := ("[b]%s[/b]：%s" % [who, text]) if who != "" else text
    _entries.append(line)
    _label.text = "\n".join(_entries)

func toggle() -> void:
    _panel.visible = not _panel.visible
```

- [ ] **Step 3: 提交**

```bash
cd 99_game
git add scripts/ui/ChoiceMenu.gd scripts/ui/Backlog.gd
git commit -m "feat(ui): ChoiceMenu 选项 + Backlog 对话历史"
```

---

## Task 13: SystemMenu + SettingsPanel

**Files:**
- Create: `99_game/scripts/ui/SystemMenu.gd`
- Create: `99_game/scripts/ui/SettingsPanel.gd`

**Interfaces:**
- SystemMenu：信号 `save_requested`、`load_requested`、`settings_requested`、`title_requested`、`closed`；`open()`/`close()`。
- SettingsPanel：文字速度/自动间隔/三通道音量/全屏，写 `user://settings.cfg`。

- [ ] **Step 1: 实现 SystemMenu**

Create `99_game/scripts/ui/SystemMenu.gd`:
```gdscript
extends Control

signal save_requested()
signal load_requested()
signal settings_requested()
signal title_requested()
signal closed()

var _panel := Panel.new()

func _ready() -> void:
    set_anchors_preset(Control.PRESET_FULL_RECT)
    _panel.set_anchors_preset(Control.PRESET_CENTER_RIGHT)
    _panel.custom_minimum_size = Vector2(360, 420)
    _panel.position = Vector2(-380, -210)
    add_child(_panel)
    var vbox := VBoxContainer.new()
    vbox.set_anchors_preset(Control.PRESET_FULL_RECT)
    vbox.offset_left = 20; vbox.offset_right = -20
    vbox.offset_top = 20; vbox.offset_bottom = -20
    _panel.add_child(vbox)
    for data in [["存档", save_requested], ["读档", load_requested],
                 ["设置", settings_requested], ["返回标题", title_requested],
                 ["关闭", closed]]:
        var b := Button.new()
        b.text = data[0]
        b.custom_minimum_size = Vector2(320, 56)
        b.pressed.connect(func(): data[1].emit())
        vbox.add_child(b)
    hide()

func open() -> void: show()
func close() -> void: hide()
```

- [ ] **Step 2: 实现 SettingsPanel**

Create `99_game/scripts/ui/SettingsPanel.gd`:
```gdscript
extends Control

const PATH := "user://settings.cfg"
var _config := ConfigFile.new()
var _panel := Panel.new()

func _ready() -> void:
    set_anchors_preset(Control.PRESET_FULL_RECT)
    _panel.set_anchors_preset(Control.PRESET_CENTER)
    _panel.custom_minimum_size = Vector2(520, 520)
    _panel.position = Vector2(-260, -260)
    add_child(_panel)
    _load()
    hide()

func _load() -> void:
    _config.load(PATH)
    # 默认值
    for k in ["text_speed", "auto_interval", "vol_bgm", "vol_sfx", "vol_voice", "fullscreen"]:
        if not _config.has_section_key("ui", k):
            _config.set_value("ui", k, 0.5 if k != "fullscreen" else false)

func get_value(key: String) -> Variant:
    return _config.get_value("ui", key, 0.5)

func set_value(key: String, v: Variant) -> void:
    _config.set_value("ui", key, v)
    _config.save(PATH)
    _apply(key, v)

func _apply(key: String, v: Variant) -> void:
    if key == "fullscreen":
        DisplayServer.window_set_mode(DisplayServer.WINDOW_MODE_FULLSCREEN if v else DisplayServer.WINDOW_MODE_WINDOWED)
    elif key == "vol_bgm" and Engine.has_singleton("AudioManager"):
        Engine.get_singleton("AudioManager").set_volume("bgm", linear_to_db(float(v)))
    elif key == "vol_sfx" and Engine.has_singleton("AudioManager"):
        Engine.get_singleton("AudioManager").set_volume("sfx", linear_to_db(float(v)))
```

> V1 设置面板只实装键值存取 + apply；具体滑杆控件在 Game 场景集成时补齐（见 Task 14 的集成说明）。

- [ ] **Step 3: 提交**

```bash
cd 99_game
git add scripts/ui/SystemMenu.gd scripts/ui/SettingsPanel.gd
git commit -m "feat(ui): SystemMenu 系统菜单 + SettingsPanel 设置"
```

---

## Task 14: GameManager + 场景（Title/Game/Ending）+ 端到端集成

**Files:**
- Create: `99_game/scripts/autoload/GameManager.gd`
- Create: `99_game/scenes/Title.tscn` + `99_game/scripts/scenes/Title.gd`
- Create: `99_game/scenes/Game.tscn` + `99_game/scripts/scenes/Game.gd`
- Create: `99_game/scenes/Ending.tscn` + `99_game/scripts/scenes/Ending.gd`

**Interfaces:**
- Consumes: 全部上述组件。
- Produces: 可运行游戏。`GameManager.goto_scene(scene_path)`、`start_new_game()`、`to_title()`。

- [ ] **Step 1: 实现 GameManager**

Create `99_game/scripts/autoload/GameManager.gd`:
```gdscript
extends Node
## 全局流程 + 键位分发。autoload 注册为 GameManager。

func _ready() -> void:
    # 初始化 Manifest 单例（供其他 autoload/UI 共用）
    if not Engine.has_singleton("Manifest"):
        var m = preload("res://scripts/data/Manifest.gd").new()
        m.load_from_path("res://data/manifest.json")
        Engine.register_singleton("Manifest", m)

func start_new_game() -> void:
    goto_scene("res://scenes/Game.tscn")
    # Game 场景 _ready 后由其调用 ScriptInterpreter.start
    call_deferred("_kick_interpreter")

func _kick_interpreter() -> void:
    ScriptInterpreter.start("chapter01_新皮肤", "桥上")

func to_title() -> void:
    goto_scene("res://scenes/Title.tscn")

func goto_ending(kind: String, title: String) -> void:
    EndingData.kind = kind
    EndingData.title = title
    goto_scene("res://scenes/Ending.tscn")

func goto_scene(path: String) -> void:
    get_tree().change_scene_to_file(path)
```

> 注：`EndingData` 需一个简单数据载体。在 `GameManager.gd` 末尾或独立文件加：
```gdscript
class EndingData:
    static var kind: String = ""
    static var title: String = ""
```
> （GDScript 2.0 支持 `class_name` 内静态变量；此处用内部 class。）

- [ ] **Step 2: 实现 Title 场景**

Create `99_game/scripts/scenes/Title.gd`:
```gdscript
extends Control

func _ready() -> void:
    set_anchors_preset(Control.PRESET_FULL_RECT)
    var bg := ColorRect.new()
    bg.color = Color(0.05, 0.05, 0.08, 1.0)
    bg.set_anchors_preset(Control.PRESET_FULL_RECT)
    add_child(bg)
    var title := Label.new()
    title.text = "他者之镜"
    title.set_anchors_preset(Control.PRESET_CENTER_TOP)
    title.position = Vector2(-120, 120)
    title.add_theme_font_size_override("font_size", 72)
    add_child(title)
    var start := Button.new()
    start.text = "开始游戏"
    start.set_anchors_preset(Control.PRESET_CENTER)
    start.position = Vector2(-80, 0)
    start.size = Vector2(160, 50)
    start.pressed.connect(GameManager.start_new_game)
    add_child(start)
    var quit := Button.new()
    quit.text = "退出"
    quit.set_anchors_preset(Control.PRESET_CENTER)
    quit.position = Vector2(-80, 80)
    quit.size = Vector2(160, 50)
    quit.pressed.connect(get_tree().quit)
    add_child(quit)
```

Create `99_game/scenes/Title.tscn`:
```
[gd_scene load_steps=2 format=3 uid="uid://title0001"]
[ext_resource type="Script" path="res://scripts/scenes/Title.gd" id="1"]
[node name="Title" type="Control"]
script = ExtResource("1")
```

- [ ] **Step 3: 实现 Game 场景（核心集成）**

Create `99_game/scripts/scenes/Game.gd`:
```gdscript
extends Control

var _bg := TextureRect.new()
var _portraits = preload("res://scripts/ui/PortraitLayer.gd").new()
var _dialogue = preload("res://scripts/ui/DialogueBox.gd").new()
var _choice = preload("res://scripts/ui/ChoiceMenu.gd").new()
var _backlog = preload("res://scripts/ui/Backlog.gd").new()
var _menu = preload("res://scripts/ui/SystemMenu.gd").new()
var _settings = preload("res://scripts/ui/SettingsPanel.gd").new()
var _auto: bool = false
var _auto_timer := Timer.new()

func _ready() -> void:
    set_anchors_preset(Control.PRESET_FULL_RECT)
    _bg.set_anchors_preset(Control.PRESET_FULL_RECT)
    add_child(_bg)
    add_child(_portraits)
    add_child(_dialogue)
    add_child(_choice)
    add_child(_backlog)
    add_child(_menu)
    add_child(_settings)
    add_child(_auto_timer)
    _auto_timer.one_shot = true
    _auto_timer.timeout.connect(_auto_advance)
    _wire_interpreter()
    _wire_ui()

func _wire_interpreter() -> void:
    ScriptInterpreter.line_ready.connect(_on_line_ready)
    ScriptInterpreter.portrait_changed.connect(_portraits.apply_slots)
    ScriptInterpreter.bg_changed.connect(_on_bg_changed)
    ScriptInterpreter.bgm_changed.connect(_on_bgm_changed)
    ScriptInterpreter.choice_presented.connect(_on_choice)
    ScriptInterpreter.ended.connect(_on_ended)
    ScriptInterpreter.chapter_finished.connect(_on_chapter_finished)

func _wire_ui() -> void:
    _choice.option_chosen.connect(ScriptInterpreter.choose)
    _dialogue.button_log.connect(_backlog.toggle)
    _dialogue.button_auto.connect(_toggle_auto)
    _dialogue.button_skip.connect(_skip)
    _dialogue.button_menu.connect(_menu.open)
    _menu.settings_requested.connect(_settings.show)
    _menu.closed.connect(_menu.close)
    _menu.title_requested.connect(GameManager.to_title)

func _on_line_ready(kind: String, payload: Dictionary) -> void:
    if kind == "say":
        _dialogue.show_line(payload.get("who", ""), payload.get("portrait", ""), payload.get("text", ""), false)
        _backlog.append(payload.get("who", ""), payload.get("text", ""))
    else:  # narrate
        _dialogue.show_line("", "", payload.get("text", ""), true)
        _backlog.append("", payload.get("text", ""))

func _on_bg_changed(scene_name: String, _time: String) -> void:
    var man = Engine.get_singleton("Manifest") if Engine.has_singleton("Manifest") else null
    var path := man.get_scene(scene_name) if man else ""
    if path != "" and ResourceLoader.exists(path):
        _bg.texture = load(path)
    else:
        var pg = preload("res://scripts/util/PlaceholderGen.gd").new()
        _bg.texture = pg.get_scene_image(scene_name)

func _on_bgm_changed(track: String, mode: String, loop: bool) -> void:
    match mode:
        "play": AudioManager.play_bgm(track, loop)
        "stop": AudioManager.stop_bgm()
        "fade": AudioManager.fade_bgm(track, loop)

func _on_choice(options: Array) -> void:
    _choice.show_options(options)

func _on_ended(kind: String, title: String, _cg: String) -> void:
    GameManager.goto_ending(kind, title)

func _on_chapter_finished() -> void:
    GameManager.to_title()

func _unhandled_input(event: InputEvent) -> void:
    if event.is_action_pressed("menu"):
        _menu.open() if not _menu._panel.visible else _menu.close()
        return
    if event.is_action_pressed("log"):
        _backlog.toggle(); return
    if event.is_action_pressed("quick_save"): _quick_save(); return
    if event.is_action_pressed("quick_load"): _quick_load(); return
    if event.is_action_pressed("auto_play"): _toggle_auto(); return
    if event.is_action_pressed("advance") or (event is InputEventMouseButton and event.pressed and event.button_index == MOUSE_BUTTON_LEFT):
        _advance()

func _advance() -> void:
    if _choice.visible: return
    if _menu._panel.visible: return
    if _dialogue.is_typing():
        _dialogue.finish_typing()
        return
    ScriptInterpreter.advance()

func _toggle_auto() -> void:
    _auto = not _auto
    if _auto: _auto_timer.start(1.0)

func _auto_advance() -> void:
    if not _auto: return
    if not _dialogue.is_typing() and not _choice.visible and not _menu._panel.visible:
        ScriptInterpreter.advance()
    _auto_timer.start(1.0)

func _skip() -> void:
    # V1：跳过 = 连续推进若干步
    for i in 20:
        if _dialogue.is_typing(): _dialogue.finish_typing()
        ScriptInterpreter.advance()

func _quick_save() -> void:
    var snap := {
        "file": ScriptInterpreter._file,
        "scene_id": ScriptInterpreter._scenes[ScriptInterpreter._scene_idx].get("id", ""),
        "line_idx": ScriptInterpreter._line_idx,
        "slots": ScriptInterpreter.slots.duplicate(true),
        "bg": "", "bgm": ""
    }
    SaveManager.save_slot(SaveManager.QUICK, snap)

func _quick_load() -> void:
    var snap = SaveManager.load_slot(SaveManager.QUICK)
    if snap.is_empty(): return
    ScriptInterpreter.start(snap["file"], snap["scene_id"])
    ScriptInterpreter._line_idx = snap["line_idx"]
    ScriptInterpreter.slots = snap["slots"]
    ScriptInterpreter._run_from_current()
```

> 注：`_quick_load` 直接访问解释器内部成员以恢复指针。这破坏封装但 V1 可接受；更干净的方式是给解释器加 `restore(snapshot)`，作为可选重构（不阻塞验收）。

Create `99_game/scenes/Game.tscn`:
```
[gd_scene load_steps=2 format=3 uid="uid://game0001"]
[ext_resource type="Script" path="res://scripts/scenes/Game.gd" id="1"]
[node name="Game" type="Control"]
script = ExtResource("1")
```

- [ ] **Step 4: 实现 Ending 场景**

Create `99_game/scripts/scenes/Ending.gd`:
```gdscript
extends Control

func _ready() -> void:
    set_anchors_preset(Control.PRESET_FULL_RECT)
    var bg := ColorRect.new()
    bg.color = Color(0.02, 0.02, 0.03, 1.0)
    bg.set_anchors_preset(Control.PRESET_FULL_RECT)
    add_child(bg)
    var label := Label.new()
    label.set_anchors_preset(Control.PRESET_CENTER)
    label.position = Vector2(-200, -60)
    label.text = "结局 [%s]\n%s" % [GameManager.EndingData.kind, GameManager.EndingData.title]
    label.add_theme_font_size_override("font_size", 48)
    add_child(label)
    var btn := Button.new()
    btn.set_anchors_preset(Control.PRESET_CENTER)
    btn.position = Vector2(-80, 80)
    btn.size = Vector2(160, 50)
    btn.text = "返回标题"
    btn.pressed.connect(GameManager.to_title)
    add_child(btn)
```

Create `99_game/scenes/Ending.tscn`:
```
[gd_scene load_steps=2 format=3 uid="uid://ending0001"]
[ext_resource type="Script" path="res://scripts/scenes/Ending.gd" id="1"]
[node name="Ending" type="Control"]
script = ExtResource("1")
```

- [ ] **Step 5: 端到端手动验收（Godot 中）**

按 README 验收点 1–5 操作。预期：标题→开始→桥上 say（占位立绘 center+打字机）→narrate 维持→choice→「再想想」经 label→释然→jump 回出租屋（换背景）→ 段尾章节结束回标题；「跳下去」→结局_BE→ending(BE) 画面。

- [ ] **Step 6: 提交**

```bash
cd 99_game
git add scripts/autoload/GameManager.gd scripts/scenes/ scenes/
git commit -m "feat(game): GameManager 流程 + Title/Game/Ending 场景端到端集成"
```

---

## Task 15: 集成回归与文档收尾

**Files:**
- Modify: `99_game/README.md`（补 GUT 全量运行说明）
- Create: `99_game/tests/.gutconfig.json`（如需）

- [ ] **Step 1: 全量跑 GUT**

Run: `godot --headless -s addons/gut/gut_cmdln.gd -gdir=res://tests -gexit`
Expected: 全部测试通过（manifest/chapter_loader/interpreter ×3/save/placeholder）。

- [ ] **Step 2: 全量跑 Python 校验**

Run: `cd 99_game/tools && python -m pytest -v && python validate_chapter.py ../data/chapters/chapter01_新皮肤.json ../data/剧本.schema.json`
Expected: 全绿 + OK。

- [ ] **Step 3: 补 README 的 GUT 安装说明，提交**

在 README「GUT 单测」小节补：从 https://github.com/bitwes/Gut 下载，把 `addons/gut/` 放进 `99_game/addons/gut/`。

```bash
cd 99_game
git add README.md
git commit -m "docs(game): 补 GUT 安装与全量运行说明"
```

---

## Self-Review（计划作者已执行）

**1. Spec 覆盖**：
- 11 条指令 → Task 4（执行分发）+ 5（jump）+ 6（立绘槽）+ bg/bgm/sfx（Task 4 分支）。✓
- 跳转寻址三层 → Task 5 `resolve_target`。✓
- 立绘累积 + 场景段 scene/bgm 套用 → Task 6 + Task 4 `_enter_scene_block`。✓
- manifest + ChapterLoader + 结构校验 → Task 2/3。✓ schema 完整校验 → Task 1 Python（离线）。✓
- 程序占位图 → Task 7。✓
- AudioManager / SaveManager → Task 8/9。✓
- 对话框/两种布局/选择/Log/系统菜单/设置 → Task 10–13。✓（两种布局：DialogueBox 不区分 is_narrate 即对话布局；is_narrate 时隐藏名称=独白；PortraitLayer center=独白、left/right=对话，已覆盖）
- 标题/结局/键位 → Task 14。✓
- 章节结束边界 → Task 4 `chapter_finished`。✓

**2. 占位符扫描**：无 TBD/TODO；Task 4 的 `_apply_portrait`/`_do_jump` 是"先占位后补"的实现步骤（Task 5/6 明确替换），非交付占位。✓

**3. 类型/命名一致性**：`ScriptInterpreter` 信号名、`choose`/`resolve_target`/`start`/`advance`/`slots`、`SaveManager.QUICK/AUTO/save_slot/load_slot/list_slots`、`Manifest.get_*`、`ChapterLoader.load_chapter` 跨任务一致。✓ GameManager.EndingData 在 Task 14 定义并在 Ending.gd 引用，一致。✓

**已知 V1 取舍（非缺陷）**：`_quick_load` 直访解释器内部成员；设置面板滑杆控件未全量实装（键值存取 + apply 已就绪）。均不阻塞验收点。
