# 待处理事项

> 记录美术生产链中需要人工跟进、审批或重做的遗留项。

---

## ⚠️ 沈暮雪 — IllusDesign 与底图 DesignSheet 未级联，重做需手工同步

- **记录日期**：2026-06-15
- **状态**：待处理
- **更新**：2026-06-15 新增第二条受影响 IllusDesign（西餐厅正式约会装 `O0Du9LWYz2`），现共有 2 条下游 IllusDesign 受同一 DesignSheet 重做风险影响

### 背景

沈暮雪（角色 ID：`NvCkQmFPFu`）的外貌被手动修改后，重做了 DesignSheet 并以其为底图，图生图生成了「电竞经理职业装」的 IllusDesign。但当时 **DesignSheet 尚未审批通过**（approve=`pending`），仍按此底图推进了 IllusDesign。

### 涉及节点

| 节点 | ID | 当前状态 |
|------|----|----------|
| DesignSheet（设计图 / 图生图底图） | `NyL21qu5Ee` | status=2, **approve=pending（未审批）** |
| IllusDesign（电竞经理职业装立绘） | `O0BFwtiWA4` | status=2, approve=pending |
| IllusDesign（西餐厅正式约会装立绘） | `O0Du9LWYz2` | status=2, approve=pending |
| CostumeStyle（电竞经理职业装） | `NyKwVuVWM4` | approve=approved（已审批） |
| CostumeStyle（西餐厅正式约会装） | `NyKwVuVWM6` | approve=approved（已审批） |

### 风险

`DesignSheet —[:produces]→ IllusDesign` 这条边 **sync=false**（不自动级联）。
**若后续 DesignSheet 在 dashboard 被驳回并重做**（例如再次调整外貌），下游这些 IllusDesign **不会自动跟随更新**，会出现：

- 立绘仍基于旧外貌底图，与新 DesignSheet 对不上
- 需要手动重新生成 IllusDesign，否则数据/画面不一致

> 编排层判定：图生图只读底图文件、不检查 DesignSheet.approve，所以当时未阻塞生成——这是隐患来源。

### 处理建议（稍后跟进）

1. 先去 dashboard 审批 DesignSheet（`NyL21qu5Ee`）：
   - 若 **通过** → 隐患消除，当前 IllusDesign 可继续用，照常推进后续。
   - 若 **驳回重做** → 重做 DesignSheet 后，必须手动重生成全部下游 IllusDesign（`O0BFwtiWA4`、`O0Du9LWYz2`）。
2. 若决定保留 sync=false：把「DesignSheet 重做后需手工重生成下游 IllusDesign」纳入流程习惯。
3. 若想根治：考虑在 skill 层为 `produces` 边增加 sync 级联或审批前置校验（产品决策，非必做）。
